<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ufabc.covidabc"
    android:targetSandboxVersion="2"
    android:versionCode="1"
    android:versionName="1.0"
    tools:ignore="GradleOverrides"
    tools:targetApi="o" >

    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>

    <uses-sdk
        android:targetSdkVersion="30"
        tools:ignore="GradleOverrides" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".login.MyAccountActivity" >
        </activity>
        <activity android:name=".login.ForgotPasswordActivity" />
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".mainScreen.MainScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainScreen.categories.event.EventDescriptionActivity" />
        <activity android:name=".mainScreen.categories.event.CreateEventActivity" />
        <activity android:name=".mainScreen.categories.faq.FAQDescriptionActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".mainScreen.categories.quiz.QuizQuestionActivity" />

        <provider
            android:name="androidx.lifecycle.ProcessLifecycleOwnerInitializer"
            android:authorities="com.ufabc.covidabc.lifecycle-process"
            android:exported="false"
            android:multiprocess="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/API_KEY"/>
    </application>
</manifest>